# Set the minimum version of CMake that can be used
# To find the cmake version run
# $ cmake --version
cmake_minimum_required(VERSION 3.5)

# Set the project name
project (subgraph_thesis)

# Add an executable
add_executable(subgraph_thesis src/main.cpp)